apiVersion: labntp.dev/v1alpha1
kind: LabNTP
metadata:
  name: x
spec:
  filters:
    - name: r
      match:
        cidrs: ["10.0.0.1/32"]
      view:
        mode: rate
    - name: default
      match:
        cidrs: ["0.0.0.0/0", "::/0"]
      view:
        mode: follow-real
