apiVersion: labntp.dev/v1alpha1
kind: LabNTP
metadata:
  name: x
spec:
  filters:
    - name: default
      match:
        cidrs: ["0.0.0.0/0", "::/0"]
      view:
        mode: follow-real
        minpoll: 10
        maxpoll: 6
