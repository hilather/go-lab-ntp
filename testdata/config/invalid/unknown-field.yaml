apiVersion: labntp.dev/v1alpha1
kind: LabNTP
metadata:
  name: x
spec:
  ntp:
    foo: bar
  filters:
    - name: default
      match:
        cidrs: ["0.0.0.0/0", "::/0"]
      view:
        mode: follow-real
