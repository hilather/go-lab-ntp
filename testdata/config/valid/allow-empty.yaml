apiVersion: labntp.dev/v1alpha1
kind: LabNTP
metadata:
  name: allow-empty
spec:
  ntp:
    allowClientCidrs: []
  filters:
    - name: default
      match:
        cidrs: ["0.0.0.0/0", "::/0"]
      view:
        mode: follow-real
