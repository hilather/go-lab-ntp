# LabNTP 1.0.0-rc.2 Release Notes

Release date: 2026-08-30
Previous release: v1.0.0-rc.1
Application version: 1.0.0-rc.2
Configuration versions: labntp.dev/v1alpha1
REST versions: /v1
MCP protocol versions: 2026-07-28
Container digest: published by tag-triggered GHCR job on this tag

## Highlights

Operator SPA at `GET /` when `spec.ui.enabled` is true: filter table enable/disable, preview-an-IP, live vs reset-only features, query ring, status, gated Reset. Cookie `labntp_session` plus CSRF `X-LabNTP-CSRF`. No localStorage tokens. Tag-triggered GHCR publish of `ghcr.io/hilather/labntp`.

## Added

- Vite/React operator UI (`docs/12-web-ui.md`), committed `internal/web/dist` embed, `make web-*`, CI job `web`.
- `.github/workflows/release.yml` tag-gate plus `publish-image` on `v*` tag push only.

## Residual

- Integrator pin in mcp-integration-lab is not this tag. Helm does not merge LabNTP. Must not block labgraph, fixture packs, mcp-integration-lab #12, or LabMITM UI.
- Host-published SPA Origin is 403 until overlay lists that origin (`allowedOrigins: []` stays).
- Host-publish UDP per-IP views still need source-preserving UDP (`userland-proxy`).
- GHCR package may be private until a human marks it public.
- linux/arm64 not in this tag.

## Deployment and operations

Image `ghcr.io/hilather/labntp:v1.0.0-rc.2` and `sha-<7 hex>`. No `:latest` on this prerelease. Integrator pin is the digest. Scratch, UID 65532, HEALTHCHECK exec form, CMD `--management-listen=:8088`. Host NTP default 10123/udp. REST 18123.

## CI and release evidence

Required CI jobs on this SHA: format, lint, unit, race, fuzz-smoke, documentation, config-compat, changelog, generated-file, parity, security-scan, container-test, web. Tag-gate requires those green before GHCR push.
